# ECN 726 Term Project: Replication and Internal Meta-Analysis
**Paper:** Fajgelbaum, P. D., et al. (2023). "The US-China Trade War and Global Reallocations."
**Student:** Mehmet Tugsuz

## 1. Project Overview
This repository contains the code and data necessary to replicate the main findings of the paper, as well as an original Internal Meta-Analysis extending the authors' baseline results.

* **Successfully Replicated:** Figure 1 and Figure 4.
* **Incomplete Replication:** Figure 2 and Figure 3. As noted in the authors' original documentation, generating these specific figures requires specialized enterprise computing resources (see Section 4).
* **Internal Meta-Analysis:** Added an original internal meta-analysis testing the sensitivity of the bystander tariff elasticity to alternative fixed-effects, lagged covariates, sectoral restrictions (Agriculture vs. Non-Agriculture), and regional heterogeneity (Asia, Europe, Americas).

---

## 2. Single-Click Execution Workflow
To bypass the 256GB RAM memory requirement needed to build the massive raw UN Comtrade files, this execution relies entirely on the pre-processed baseline datasets (`data5.dta` and `rf.dta`) located in the `/data/processed/` folder.

To fulfill the single-click format requirement, please update the global directory path in `code/00_directories.do` to match your local machine. Then, simply execute the do-files in the following sequence from the Stata command window:

### Step 1: Baseline Replications
```stata
cd "/path/to/your/folder/GTW_replication/code"
do 00_directories.do
do didplots.do
do quadplot.do
```

Result: `fig_1.pdf` and `fig_4.pdf` will be successfully generated and saved in the `/results/` folder. (Note: Do not run `winners.do` unless you have a 64-core Linux server, as it will crash during the bootstrap merge).

### Step 2: Internal Meta-Analysis Execution
Once the environment is set up, run the custom meta-analysis scripts:

```stata
cd "/path/to/your/folder/GTW_replication/code"
do 02_meta_analysis.do
do 03_regional_meta_analysis.do
```

Result: These scripts will generate `meta_results.dta` and `meta_results_region.dta` in the `/data/processed/` folder. They will also automatically output `meta_analysis_forest_plot.png` and `regional_forest_plot.png` into the `/results/` folder.

## 3. Storage of Results
All graphical outputs and tables generated by both the replication scripts and the internal meta-analysis scripts are automatically exported and saved in the root `/results/` directory.

## 4. Detailed Explanation for Incomplete Replication
Per the ECN 726 project guidelines, this section explains why Figures 2 and 3 could not be replicated locally.

The authors' code constructs the maps for Figures 2 and 3 by calculating confidence intervals through a 50-iteration bootstrap loop (requiring the merging of `bs1.dta` through `bs50.dta`).

**The Hardware Limitation:** The authors explicitly state that executing these bootstraps requires a high-performance Linux server equipped with 64 cores and 256 GB of RAM.

Because a standard local computer cannot support this extreme memory threshold, the bootstrap datasets cannot be generated. Consequently, the `winners.do` script halts execution during the merge loop, preventing the creation of the final map PDFs.

**Impact on Main Results:** It is important to note that this hardware limitation only prevents the calculation of the bootstrapped confidence intervals and map extensions. It does not affect the baseline point estimates, which have been successfully executed, verified, and utilized for the internal meta-analysis.